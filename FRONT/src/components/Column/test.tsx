import { screen, render } from '@testing-library/react'
import Column from '.'

describe('<Column />', () => {
  it('should render the heading', () => {
    // const { container } = render(<Column />)
    // expect(screen.getByRole('heading', { name: /Column/i })).toBeInTheDocument()
  })
})
