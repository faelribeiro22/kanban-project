import { screen, render } from '@testing-library/react'
import Kanban from '.'

describe('<Kanban />', () => {
  it('should render the heading', () => {
  //   const { container } = render(<Kanban />)
  //   expect(
  //     screen.getByRole('heading', { name: /Kanban/i })
  //   ).toBeInTheDocument()

  //   expect(container.firstChild).toMatchSnapshot()
  // })
})
